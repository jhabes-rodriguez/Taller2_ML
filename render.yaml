services:
  - type: web
    name: premier-predictor-ai
    runtime: static
    buildCommand: echo "Static site - no build needed"
    staticPublishPath: ./Dashboard
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=0, must-revalidate
